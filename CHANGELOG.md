# EAHN-HiDF Phase-by-Phase Changelog

Single source of truth for every architectural change shipped to date.  Use
this when writing the ablation section of the paper — each row is one
self-contained increment with its rationale, the metric outcome it produced
on a real Kaggle run, and the commit hash that landed it.

> **How to use this file**
> - Add a new row whenever a numbered Phase ships to GitHub.
> - Update the "outcome" column only after the next Kaggle run completes.
> - Keep one paragraph of "what we learned" so the ablation story is honest.
> - Never edit a past row except to add the post-run outcome — history is
>   what makes ablations credible.

---

## Phase ledger

| Phase | Date | Commit | Headline change | HiDF AUC | CelebDF AUC | Insertion AUC | Faithfulness corr | k1 ratio | Status |
|---|---|---|---|---|---|---|---|---|---|
| 0 | (origin) | — | EfficientNet-B4 + 4-layer Transformer + softmax M_t at 7×7, classifier = Linear(d→1) over mean-temporal-pool | — | — | — | — | — | baseline |
| 18 | 2026-05-?? | (pre-history) | Class-symmetric augmentation, focal loss, label smoothing 0.01 | — | — | — | — | — | pre-explanation work |
| 19 | 2026-05-?? | — | Balanced 100R+100F CelebDF/FF++ cross-eval; per-manipulation FF++ split | — | — | — | — | — | eval-only |
| 20 | 2026-05-?? | — | `attn_floor` raised so M_t can never be exactly zero; Gaussian-blur fill in insertion/deletion metric | — | — | — | — | — | metric hygiene |
| 21 | 2026-06-?? | — | **EarlyAttnHead** generates M_t from CNN features BEFORE transformer; M_t gates spatial tokens going into transformer; bottlenecked input + faithfulness KL loss | — | — | — | — | — | core attention path |
| 22 | 2026-06-?? | — | `bottleneck_peak_floor=0.25`: prevents diffuse M_t from making bottleneck identical to original; KL loss starts biting | — | — | — | — | — | bottleneck pressure |
| 23 | 2026-06-08 | `922b5cd` | **temporal_gate bottleneck** — Linear(d→d/4)→Linear(d/4→1)→softmax over T replaces `.mean(dim=1)`; `attn_floor 0.05→0.0` (true spatial bottleneck); moderated aug strengths | 0.904 | 0.66 | 0.249 | 0.18 | 1.64 | partial win (insertion regressed) |
| 24 | 2026-06-09 | `065129e` | **loss_ins** = focal-CE on bottleneck-forward logits, re-uses existing `out_B` (zero extra forward pass); `lambda_ins=0.5` with 3-epoch warmup; insertion-AUC training objective | 0.825 | 0.649 | 0.499 | 0.225 | 0.97 | detection -8pts; insertion +25pts |
| 25 | 2026-06-09 | `a6597bd` | **Bi-directional refinement**: cross_attention rewired (was discarded `_legacy_`), learnable `refine_gate` blend; **hard top-K binary bottleneck mask** (straight-through, K=20%); `lambda_ins 0.5→1.0`, `lambda_sparse 0.05→0.10`, `alpha→0`, `beta→0.1`; **temporal_sparsity_loss** on M_frame; `lambda_temp_sparse=0.05` | 0.708 | 0.535 | 0.554 | **0.117** | 0.66 | **REGRESSED** — too many knobs at once; M_t over-collapsed; real_acc fell to 0.14 |
| 26 | 2026-06-10 | `dead521` | **Revert** Phase 25 amplifications to Phase 24 values; **`refine_gate` init -2.0→-0.5** so α≈0.38 from epoch 1 (Phase 25 never engaged the bidirectional path); **WeightedRandomSampler** to combat the 14% real-class collapse seen in Phase 25; **Concept Slot Bottleneck (CBM)** — K=8 learned slot queries, **parallel** classifier head, slot-diversity loss | **0.800** | 0.618 | 0.499 | **0.079** | 1.000 | partial — detection recovered, real_acc 0.689 (BEST), but **parallel CBM gave the model an escape hatch** → M_t decoupled from prediction → faithfulness regressed to worst-ever value, k_ratios flat-uniform. FF++ FaceSwap fell to 0.439 (below random). |
| 27 | 2026-06-11 | `a60dd31` | **Serial CBM** — out.logit IS cbm_logit; main_logit kept only as aux-supervision regulariser via `lambda_cbm_main_aux=0.05`; **K bumped 8 → 12** for serial expressiveness. **DANN domain adversarial training (DIAT variant)** — 4 synthetic domains via augmentation (clean / heavy-JPEG / noise / blur), per-sample random domain at `__getitem__`, GRL on attn_pool, DomainHead MLP (~34k params, ~0.7% of model). Linear warmup of `lambda_grl` 0→1.0 and `lambda_domain` 0→0.10 over 3 epochs. Detailed rolling-log + first-batch DIAG-P27 prints + 3 new history columns (`train_domain`, `train_domain_acc`, `train_cbm_main_aux`) for attribution. | **0.914** | 0.593 | 0.541 | 0.071 | 1.000 | split verdict — **detection best-ever (0.914, +0.114 vs P26)**, training stable for the first time since P23, DANN engaged exactly as designed (dom_acc → chance 0.25, L_domain pinned at ln 4). But explanations stayed dead because the serial CBM reads **RAW transformer Q**, bypassing both the M_t-weighted pooling and the M_frame temporal gate — run log proved M_frame was 99.998% one-hot on one frame yet dropping that frame ≡ dropping a random frame. The escape hatch wasn't removed in P27; it moved. FF++ FaceSwap fell further to 0.381 — DANN over processing domains cannot teach manipulation-method generalisation. Two metric confounds also identified: zero-fill frame drop = +0.37 fake-prob artifact for ANY masked frame; blurred clips score 0.82 fake vs 0.47 baseline so del/ins curves invert regardless of map quality. |
| 28 | 2026-06-11 | `77b0c69` | **CBM-attention coupling** — CBM input tokens scaled by `w = M_t ⊙ M_frame` (max-normalised per sample, `cbm_coupled=True`, `--no_cbm_coupled` ablation): the explanation maps become load-bearing for the prediction, so cls loss directly punishes dishonest maps (P23 precedent: coupled path gave 0.904 AUC + k1 1.64 + faith 0.18). **Metric protocol fixes (eval-only)**: frame-drop test fill changed zero → nearest-frame **replication** (legacy zero-fill kept under `*_zerofill` keys); del/ins gains a **random-saliency control** (`ins/del_gain_over_random` — robust to the blur-direction artifact), fake-only aggregates, chunked forwards, and bigger samples (evaluate 5→20 clips, suite 10→50, steps 10→20). `--lambda_cbm_aux 0.0` (in serial mode it duplicated loss_cls → cls double-counted at 1.10). New diagnostics: `[DIAG-P28]` first-batch print + rolling `eff_tok=` + history column `train_eff_tokens` (effective token count of w; watch: collapse → ~1 means bottleneck too brutal, stuck at T·N means coupling not biting). | 0.541 | 0.470 | 0.500 | (noise) | (noise) | **CATASTROPHIC — training never started** (run 6-11-26 1300hrs). Output constant at prob ≈0.4996 (= sigmoid of the CBM fc bias) for all 9 epochs: `cls` frozen at 0.1838 from batch 1000, val AUC never left ~0.5, every test clip predicted "real" (tp=0). Root cause: **scaling token MAGNITUDES is not pooling** — once the sparsity regularisers made the maps peaky, 782/784 CBM keys were near-zero, the slot softmax diluted the survivors (~780 dead keys at e⁰ each soak up the mass), `slot_pool` collapsed to ~0, the logit froze at the fc bias, the cls gradient died, and the regularisers ground `eff_tok` from 56 → 2 unopposed (one-way ratchet; `tsparse` reward hit −0.98 by end of epoch 1, `cbm_div` pinned at 0.9995 = all 12 slots identical). The metric protocol fixes (replicate fill, random-saliency control) are KEPT — they are eval-only and orthogonal to the collapse. The k1=2.35/faith=0.33 numbers in that run's files are noise on a constant function — never quote them. |
| 29 | 2026-06-11 | `b9b864a` | **Pooled-frame CBM — repair the coupling** (`cbm_pooled=True`, `--no_cbm_pooled` ablation; supersedes `cbm_coupled`). The CBM reads `attn_pool_per_frame` (B, T, d) — the M_t-pooled per-frame vectors. A **convex combination**: full magnitude however peaky M_t gets, and tokens M_t suppresses are structurally ABSENT from the prediction (spatial coupling cannot be bypassed). M_frame couples as a **log-space attention prior** inside the CBM (`attn_logits += log(M_frame)`, renormalised by the softmax — the temporal coupling cannot starve magnitudes either). Both couplings are renormalised forms, so the P28 starvation mode is structurally impossible. **`lambda_temp_sparse 0.02 → 0.0`** — it drove M_frame to 99.998% one-hot in P27 and was the P28 collapse accelerant; P23 had no temporal sparsity reward and still hit k1 1.64 because a load-bearing M_frame concentrates itself. New diagnostics: `[DIAG-P29]` (incl. `logit_std` constant-output warning — the P28 signature), rolling `eff_fr=`/`eff_sp=`/`s_top=` (slot mass on top-M_frame frame = prior-adherence / escape-hatch detector), history columns `train_eff_frames`/`train_eff_spatial`/`train_slot_on_top`. Smoke gate now includes a 30-step mini-overfit (loss must decrease — would have caught P28 pre-push). P23 precedent for this path: 0.904 AUC / k1 1.64 / faith 0.18 under the old confounded protocol; P29 adds the K=12 slot readout + sampler + DANN on top. | 0.879 | 0.647 | 0.401 | −0.017 | 5.12 (repl.) | **training REPAIRED, explanations still unfaithful** (run 6-12-26 0020hrs). Best-ever cross-dataset across the board (CelebDF 0.647, DF 0.672, NT 0.655); val AUC climbed every epoch to 0.884 (best = E9, the LAST epoch — undertrained); bal_acc@opt 0.801. Three remaining root causes, all log-visible: (1) **slot collapse** — `cbm_div` pinned 0.98–1.0 the whole run; `slot_q` init 0.02 makes slot logits ±0.02 vs the log-prior's ±1–2, so all 12 slots reduce to the prior's shape → effective K=1 (also caps detection: 0.879 went through one effective scalar). (2) **No necessity signal** — B-pass trains sufficiency only; deletion AUC 0.502 was WORSE than the 0.436 random control (evidence redundant outside the map); faith corr ≈ 0. (3) **M_frame near-uniform** (`eff_fr` 10–13/16, nothing pushes temporal selectivity at `lambda_temp_sparse=0`) → top frame 13% mass → k-drop absolutes ~0.0015 = noise; the k1 ratio 5.12 is real but fragile (k2 ratio −22 = near-zero denominator artifact, not signal). `alpha` never moved off 0.373–0.374 (bidir refine gate inert since P25 — gets no usable gradient). |
| 30 | 2026-06-12 | `b015141` | **Faithfulness closure — slot repair + necessity pass + bounded band** (user-directed consolidation of three repairs; each has an off-switch + dedicated log diagnostic for attribution — see Standing-rule note). (1) **Slot-scale repair**: `slot_q` init `N(0,0.02²) → N(0,1)` so slot logits compete with the log(M_frame) prior from batch 1 (random 256-d queries are near-orthogonal, E[cos]≈0.06); `lambda_cbm_div 0.05 → 0.15`. Smoke: init `cbm_div` 0.67 (was pinned 1.0). Watch: `cbm_div` < 0.9 by E2 and falling. (2) **Deletion (necessity) D-pass** `--lambda_del 0.3` (0.0 = off): `build_bottlenecked_input(invert=True)` — top-M_t region REPLACED by blur, rest visible — classified toward REAL for all samples (real: blur ≠ fake; fake: erasing the marked evidence must remove fake-ness → ALL decisive evidence must live under the map). Alternates with the B-pass on step parity, so the per-step forward count stays 2 (grad_accum 8 → every optimizer step averages 4 B + 4 D batches). The map is pinned from both sides: B-pass = sufficiency, D-pass = necessity; the "M_norm→0 everywhere" degenerate solution is blocked by `bottleneck_peak_floor` exactly as in the B-pass. Shares `faith_warmup_epochs`. (3) **Bounded temporal band** `--lambda_temp_band 0.05 --temp_band_target 6.0` (0.0 = off): hinge on eff_fr = 1/Σp² — penalty above 6 effective frames, ZERO gradient below, so it concentrates M_frame to ~6 frames and then lets go (structurally cannot ratchet to one-hot — the P27 `lambda_temp_sparse` failure; that flag stays 0.0). EPOCHS 9 → 10 (P29 val still climbing at E9; ~10.5h of the 12h cap). **Eval changes**: XAI overlays 10 → 50 videos (25R+25F, 2:2:1 high/mid/low confidence per class, frames GPU-resident; `--xai_overlay_videos`), `--heatmap_samples 50` (faith/SSIM subset 20 → 50; evaluate-side del/ins cap 20 → 50 to match the suite), **faithfulness_corr fixed to the standard per-sample Spearman mean** (the old pooled-flatten version correlated across samples, polluted by between-sample |gradient| scale — see Metric protocol note). New diagnostics: `[DIAG-P30]` (slot_q_std + low-scale WARNING, L_cbm_div at init, lambda echo), rolling `del=`/`tband=`, history `train_del`/`train_temp_band`. | 0.796 | **0.683** | 0.437 | **+0.267** (new per-sample protocol) | −19.2 (repl., noise) | **every mechanism fired — detection paid for it** (run 6-12-26 1300hrs, 11.1h total). Slot collapse FIXED (`cbm_div` 0.94→0.10, slot_q_std 0.998); temporal band landed (eff_fr 4.3–4.6, `tband`→0.014); `del` fell all run; faith corr positive for the FIRST time (+0.267 vs −0.017); `del_gain_over_random` +0.098 (first real necessity signal); CelebDF best-ever 0.683; map quality probes all healthy (inter_sample_cos 0.31, peak_mode_share 0.034, mt_vs_random_model_cosine 0.069). BUT test AUC 0.879→0.796, fake_acc@0.5 0.569 with **train_clean fake_acc 0.40** and val fake_acc 0.082 at E4 — the first full-strength `lambda_del` epoch. Root causes, log-proven: (1) **D-pass label poisoning** — BCE-to-0 demanded CERTAIN-REAL on fakes whose erased footprint was ~2/49 cells (96% of the face still visible) on HALF of all steps = systematic "visible fake evidence → REAL" supervision; score distribution REAL-shifted (optimal_threshold 0.46); insertion curve DESCENDS with reveal fraction (0.494@10% → 0.402@50%) because sharp pixels were trained to read REAL. (2) **Spatial ratchet** — `lambda_sparse` (−peak, open-ended) squeezed eff_sp 3.4 (P29, AUC 0.879) → **1.8 cells** (P30): detection starved, insertion ordering carried ~2 cells of signal (`ins_gain_over_random` −0.061). (3) **Focal `alpha` is a pure global scale** (multiplies both classes) — fake recall never had a loss-side counterweight (P29 fake@0.5 0.497, P30 0.569, real ~0.86 both runs). (4) **k-drops physics-capped** — replicate fill copies a neighbour frame back in; on fully-fake temporally-redundant clips the neighbour carries the same evidence, so single-frame necessity ≈ 0 for ANY map (P29 +5.12 / P30 −19.2 = the same coin flip). Protocol insight: mixed-class absolute ins/del AUCs are bounded by design — a LOW insertion curve is CORRECT for real samples — quote `*_fake_only` + `*_gain_over_random`. |
| 31 | 2026-06-12 | `9340f4f` | **D-pass detox + spatial band + class-conditional focal + blur-fill drops** (repair-of-the-repair under the same user directive; every axis keeps an off-switch + log signature). (1) **D-pass detox** `--lambda_del 0.2 --del_margin_logit 0.0 --del_anchor_every 8`: fakes get hinge `relu(logit_D − margin)` ONLY when the A-pass currently detects them (`sigmoid(logit_A)>0.5`) — pushes p(fake|evidence erased) to ≤0.5 then STOPS (zero gradient at the margin: structurally cannot shift the global score prior, unlike BCE-to-0 which kept pushing toward certainty-REAL); reals keep BCE-to-REAL (their TRUE label — blur-spot augmentation that also kills the blur→fake shortcut). Every 8th batch (8k+7 is always odd → strict subset of D-steps; exactly one per optimizer step) is a **FULL-blur ANCHOR**: `model(blur(x))` → REAL for ALL samples — no evidence visible ⇒ REAL is epistemically correct for both classes; anchors the blur end of the del/ins curves (P30 `blurred_conf` 0.40 floored deletion AUC ≈0.40) while never pairing VISIBLE evidence with a REAL label; the B-pass trains the opposing case (blur canvas + sharp top region → TRUE label) so blur itself cannot become a class marker. (2) **Spatial band** `--lambda_spatial_band 0.05 --spatial_band_lo 4.0 --spatial_band_hi 10.0` + `--lambda_sparse 0.0`: two-sided hinge on per-frame eff_sp = 1/Σp² over 49 cells, ZERO gradient inside [4,10] — the spatial mirror of the P30 temporal band that worked; compatible with `bottleneck_peak_floor` 0.25 (eff 4–5 with peak ≥0.3 is feasible). `sparsity_loss` retired to diagnostic-only (same open-ended-ratchet pathology as P27's temporal −max). (3) **Class-conditional focal** `--focal_alpha_pos 1.0 --focal_alpha_neg 0.5` (legacy `--focal_alpha` path kept; bit-exact back-compat when pos/neg unset): standard alpha_t form — 2:1 fake:real error weighting at the same mean scale as the legacy global 0.75. (4) **Blur-fill frame drops** (eval-only, +~6 min): third fill protocol `k{K}_*_blurfill` — Gaussian-blurs the dropped frames (same kernel as the del/ins protocol), DESTROYING their evidence instead of replicate-fill copying it back in from a neighbour; the only fill that can show a top-vs-random differential on fully-fake clips; replicate/zero kept for cross-phase continuity. KEPT from P30 (verified working): slot_q init 1.0, `lambda_cbm_div` 0.15, temporal band 0.05/6.0, EPOCHS 10, 50-video GPU eval, per-sample-Spearman faith corr. Smoke: 11 gates — exact focal back-compat, hand-computed band hinges (uniform→1.0, one-hot→0.75, in-band→0), B/D-erase/D-anchor schedule, D-erase gradient reaches early_attn while anchor input is graph-free, `*_blurfill` keys, full notebook flag-set parse, 30-step mini-overfit with the complete P31 stack (cls 0.075→0.0009). New diagnostics: `[DIAG-P31]` (sband at init, band edges, del form, alpha_pos/neg, sparse/band conflict WARNING), rolling `sband=`, history `train_spatial_band`. Watch: `sband` falling with eff_sp settling 4–6 (NOT 1.8); `del=` small and falling after E3 with **val fake_acc NOT collapsing at E4** (the P30 signature); val AUC tracking ≥ P29's trajectory (0.64/0.70/0.72…), not P30's (0.56/0.64/0.67…); detection ≥0.87, stretch 0.91. | 0.9115 | 0.6314 | 0.296 (fake-only 0.388) | 0.318 | 1.19 (blurfill) | **detection + necessity FIXED; sufficiency still gapped** (run 6-13-26 0900hrs, 10.4h). Every P30 root cause fixed with the predicted log signature: val fake_acc at E4 = 0.555 (NO collapse — the P30 poisoning signature gone), eff_sp settled ~5 in-band (was 1.8), fake_acc 0.569→0.788, blurred_conf 0.40→0.3245 (full-blur anchor worked), cbm_div 0.95→0.20. Deletion 0.437→**0.224** with **del_gain_over_random +0.285** — necessity faithful on EVERY one of 50 samples. AUC 0.796→**0.9115** (val peaked 0.924 @E9). BUT insertion ordering still LOSES to random (**ins_gain_over_random −0.216**, universal across all 50 clips): revealing the top-attention 10% on a blur canvas DROPS confidence (0.3245→0.2808) = necessary-but-not-sufficient = distributed (global) evidence, compounded by a train/eval mismatch (loss_ins trains the SOFT bottleneck; the metric does a HARD pixel reveal). faith corr +0.318 (up from +0.267, short of 0.55). k-drops: blur-fill stabilised the sign (k1 1.19, k2_zerofill 2.03) but magnitude is tiny — single-frame drops are physics-limited on 16-frame redundant clips (k≥2.5 retired). CelebDF dipped 0.683→0.631 (in-dist gains traded a little transfer; needs SBI/P33). The "EXPLANATION COLLAPSE m_t_std 0.047<0.05" log warning is a FALSE alarm — geometrically incompatible with the spatial band's own eff_sp target (softmax over ~5–8 effective cells caps std near 0.05). |
| 32 | 2026-06-13 | `4c96a87` | **B-pass hard-top-k sufficiency — insertion/faithfulness alignment** (user-directed: "fix the explanation metrics to publish"). P31 verdict: insertion ordering loses to random because `loss_ins` trains the SOFT bottleneck (peak_floor path keeps the whole region at low contrast) while the insertion metric does a HARD top-k pixel reveal — the model was never trained on what it is tested on. Fix: re-enable the hard top-K binary keep-mask (P25's straight-through estimator) for the **B-pass ONLY** via new `--ins_hard_topk_frac_lo 0.10 --ins_hard_topk_frac_hi 0.25` — frac sampled U[lo,hi] per B-step so `loss_ins`/`loss_faith` train the steep 10–25% reveal span (the reported insertion checkpoints), not a single point. The D-pass keeps `--bottleneck_hard_topk_frac 0.0` (soft), so the met deletion (0.224) is structurally untouched. P25's hard-mask collapse cause (class imbalance → always-fake, real_acc 0.14) is now removed by the WeightedRandomSampler (P26) + class-conditional focal (P31) — the failure condition no longer exists. Couples insertion (direct) + faithfulness (gradients concentrate on the now load-bearing top cells → M_t↔grad agreement rises). Single axis; off-switch `lo=hi=0`. New `[DIAG-P32]` first-batch print (sampled frac, K pixels, L_ins). Detection-protective: A-pass cls every step + sampler + class focal + spatial-band floor + 3-epoch warmup; checkpoint is detection-selected (balanced_accuracy_at_optimal) so the saved model keeps the best-AUC epoch even if late epochs over-localise. Risk (P24 precedent): −3 to −8pts detection for the insertion gain; headroom from 0.91. | 0.901 | 0.658 | 0.280 (fake-only 0.384) | 0.260 | 0.79 (blurfill) | **partial — moderate kept detection, sufficiency still gapped** (3-account sweep, run 6-14-26 1015hrs). Swept the keep fraction across 3 accounts: **moderate [0.10,0.25]** (the shipped default) reproduced P31 detection (**0.901**, real 0.897 / fake 0.768), best CelebDF (0.658), met deletion (0.194, del_gain +0.197); **aggressive [0.05,0.12]** and **gentle [0.20,0.35]** both DROPPED detection to ~0.825-0.829 and destabilised real_acc (gentle bounced 0.54→0.09→0.34 across epochs). The hard-mask HALVED the insertion deficit (ins_gain_over_random −0.216 → −0.075..−0.106) but never crossed 0 on ANY of the three — and the absolute insertion rises in aggressive/gentle move TOGETHER with deletion (0.19→0.43), a confidence-baseline shift, not localization. Per-sample log: deletion-gain positive on ~42/44 clips (necessity holds) but insertion-gain negative on ~35/44, and revealing RANDOM pixels (ins AUC 0.39) beats revealing the top-attended ones (0.28). Conclusion: the keep-fraction knob is exhausted — insertion-beats-random is structurally unreachable for a HOLISTIC detector by tuning losses; the evidence must be made LOCAL (→ Phase 33). |
| 33 | 2026-06-14 | `550eecd` | **Self-blend (SBI) + boundary-supervised attention — the local-evidence fix** (user-directed: "maximum hard structural fix, must work first time, do not break detection / cross-dataset / session limit"). P32 sweep PROVED the insertion wall is structural: the detector's fake-evidence is HOLISTIC (delete top region → fake-conf 0.45→0.10 necessity; insert it on blur → 0.23→0.27 not sufficient; random reveal beats attention), so no compact map can be sufficient and insertion loses to random by design. Fix = give the model a LOCAL, causal target. New `data/HiDF_self_blend.py` (`make_sbi_batch`, GPU, fp32, no new deps): each optimizer step a small batch of REAL clips is self-blended on-GPU into pseudo-fakes — a mildly warped (affine grid_sample) + colour/contrast-shifted copy composited under a soft convex mask, leaving a misaligned blend SEAM (the boundary, temporally consistent across all 16 frames). New `localization_loss` (losses/HiDF_explanation.py): soft cross-entropy pulling M_t onto the downsampled seam (boundary = m·(1−m)); the seam is sufficient by construction so insertion in attention order recovers conf fast, and faith rises (grad↔attention agree on the seam). Self-blends are also the SOTA cross-dataset cue (Face X-ray / SBI). Flags: `--sbi_enabled --lambda_localize <X> --lambda_sbi_cls 0.5 --sbi_blend_lo 0.25 --sbi_blend_hi 0.55 --sbi_stride 8`; OFF = exact P32. **Detection-protective by construction**: the A/B/D detection passes are UNTOUCHED; SBI is a bounded ADDITIVE aux pass run once per optimizer step with its OWN backward AFTER the main backward — so its forward graph never coexists with A/B (peak VRAM stays at the A+B level, no OMM cost) and lambda_localize lands at full per-step weight. Runtime cost ≈ +1 A-pass-equivalent / optimizer step ≈ +6% (~+0.5h → ~11h, within the 12h cap). Built on the proven detection-safe moderate config (hard-topk [0.10,0.25]). Single axis = `--lambda_localize`; 3-account sweep 0.5 / 1.0 / 2.0. New `[DIAG-P33]` first-batch print + `[P33]` per-epoch `mean_L_localize` (watch it FALL = M_t learning the seam). Smoke: component + real-EAHN e2e (grad to EarlyAttnHead 93.8, no non-finite grads) + config parse, all green. Honest caveat: K-drops stay ~1 on temporally-uniform HiDF (data-limited, not fixable by training). | 0.971 (EXP3) | 0.566 | 0.295 (fake-only 0.431) | 0.370 | 1.48 (blurfill) | **SBI = detection breakthrough; insertion still structurally gapped** (3-account lambda_localize sweep 0.5/1.0/2.0, run 6-15-26 0800hrs, EXP1/2/3). The SBI fake-classification term (`L_sbi_cls` 0.10→0.01 every run) was the strongest detection regulariser yet AND stabilised training: detection scaled MONOTONICALLY with lambda_localize — EXP1 0.916, EXP2 0.957, **EXP3 (λ2.0) 0.971 / fake_acc 0.816 = best-ever**, no collapse (EXP3 val AUC 0.835→0.974 monotonic). BUT seam-localization did NOT make HiDF insertion beat random: `L_localize` fell only 0.92→0.69 (unit-normalized; never localized sharply) and `ins_gain_over_random` got WORSE as λ rose (−0.085 / −0.224 / −0.233; negative on 42/50 samples) — the SBI seam location ≠ the holistic HiDF evidence location, so pulling M_t to the seam prior moved it AWAY from real evidence (lower λ = least harm). Necessity preserved (del_gain +0.10..+0.26; deletion 0.25 / 0.50 / 0.28 — EXP2's drifted high). Faith improved 0.318→0.42/0.45/0.37 (short of 0.55). Cross-dataset flat ~0.61 (CelebDF dipped to 0.53–0.59). K-drops still data-limited (k1 blurfill 1.39/2.06/1.48). Verdict: KEEP SBI-as-classification (the detection win), DROP seam-localization-as-prior (backfired on insertion) → Phase 34 spends the surplus on a forward spatial bottleneck. EXP3 (λ2.0) is the go-forward base; EXP2 (λ1.0) retired as dominated. |
| 34 | 2026-06-15 | de6ef2d | **Hard spatial top-k attention bottleneck — the architectural insertion fix** (user-directed: chose "push insertion" over banking the detection win). P33 left a detection surplus (0.971 vs 0.92 target) but insertion stayed structurally stuck because the model average-pools holistic evidence — no compact map can be sufficient by tuning (proven P31/P32/P33). Fix = make the architecture USE a compact region: at the M_t spatial-pool step (`models/HiDF_eahn.py::_spatial_bottleneck`), keep ONLY the top `spatial_topk_frac` of the 49 cells (top-k mask + convex renorm + straight-through estimator), so `attn_pool_per_frame` — which feeds BOTH the classifier AND the serial CBM (Phase 29) — funnels the WHOLE prediction through those cells. The model must concentrate real evidence into the kept cells → revealing them (insertion) beats random and faithfulness rises with it. Convex renorm preserves pooled magnitude (the P28 anti-starvation lesson); STE passes gradient to ALL cells (suppressed cells keep learning). Lives in `forward`, so it applies identically at train AND eval (`run_evaluation` rebuilds `EAHN(config)`; the bottleneck adds ZERO parameters so checkpoints stay fully compatible). New flag `--spatial_topk_frac` (0.0 or ≥1.0 = OFF = exact P33, byte-identical). Funded by the P33 detection surplus; built on the EXP3 winning base (lambda_localize 2.0, SBI on, hard-topk [0.10,0.25]). Single axis; 2-account sweep frac **0.15 (keep 8/49)** / **0.10 (keep 5/49)** — grounded BELOW the model's converged eff_sp ~10–13 (EXP3 log: 23.9→13.3 and falling) so the bottleneck bites, ABOVE the spatial-band lower edge 4 so it doesn't fight the band. New `[Phase34]`/`[DIAG-P34]`/`[P34]` prints (watch `mean_kept_mass` RISE across epochs = M_t concentrating into the kept cells = locally sufficient). Smoke: 24 gates across 2 scripts (off-switch byte-identity, exact-k selection, convex renorm sum-to-1, kept-set==top-k, STE gradient to early_attn + backbone, output fields, AMP/bf16, e2e forward+backward+step at 0.25/0.50, config argparse map, ASCII cp1252-safe prints) — all green. Detection-protective: STE keeps gradient to all cells + A-pass cls every step + sampler + class focal + detection-selected checkpoint; surplus absorbs the locality cost. | 0.961 (EXP2) | 0.552 | 0.414 (fake 0.573) | 0.498 | noisy (k2/k4 +ve) | **NET REGRESSION — revert (run 6-16-26 0000hrs, EXP1 keep8/49 & EXP2 keep5/49).** The bottleneck bit as designed (`mean_kept_mass` rose 0.54→0.75 / 0.43→0.58) and moved the sufficiency side (`ins_gain` −0.233→−0.139/−0.123, insertion fake-only 0.43→0.46/**0.573**, faith 0.370→0.476/**0.498**) — but at the cost of the metrics that were already winning: deletion 0.277→0.46/0.44, **del_gain +0.256→+0.04/+0.10** (the in-model top-k re-selects surviving cells on a deletion-perturbed input → robust to deletion → LESS necessary), detection 0.971→0.963/0.961, cross DF 0.787→0.717/0.745 — and `ins_gain` STILL never crossed 0. keep5 (EXP2) beat keep8 (EXP1) on nearly every explanation metric (tighter = monotonically better). **BIG FINDING (full 4-config sweep): a single map cannot be both necessary AND sufficient on holistic fakes** — `lambda_localize` trades necessity↑/sufficiency↓ (P33 EXP1 λ0.5: ins_gain −0.090 best-ever, del_gain +0.095; EXP3 λ2.0: −0.233 / +0.256), the bottleneck trades the other way, neither crosses ins_gain=0. → Phase 35 dual-lens; revert the bottleneck. |
| 35 | 2026-06-16 | 2bb0d2e | **Dual-lens (necessity + sufficiency maps) + multi-artifact generator + eval upgrades** (user-directed: "fix ALL metrics, not just insertion or deletion"). P34 proved a single map can't be both necessary and sufficient on holistic fakes, so split the job across TWO complementary attention maps. **M_nec** = the existing M_t path (seam prior + D-pass, NO bottleneck) carries necessity + cross-dataset; **M_suff** = a second `EarlyAttnHead` (bottlenecked by `--suff_topk_frac 0.10`, B-pass sufficiency + faithfulness, NO seam) carries sufficiency + gradient-faithfulness. The classifier reads a LEARNABLE sigmoid blend of BOTH per-frame pools (init 0.5) so neither map can decay into decoration (`models/HiDF_eahn.py`; gate `--dual_lens_enabled`). **Enhanced generator** (`data/HiDF_self_blend.py`): artifact MODES (`--sbi_modes blend,warp,color` — warp=reenactment→Face2Face/NeuralTextures, color=graphics-swap→FaceSwap = the cross-dataset axis) + TEMPORALLY-PARTIAL fakes (`--sbi_partial_frac_lo/hi` — manipulate k of 16 frames → a real key frame so the k1/k2/k4 drop test is meaningful). **Eval upgrades** (zero training risk): insertion ranks by M_suff / deletion by M_nec (`saliency_ins`), `localization_loss` frame-weighted for partial fakes, alternate insertion baseline (`--insertion_baseline mean/black` lifts the blur-capped number). ALL flags OFF = byte-identical P33/34 (verified: single-lens aliases M_suff→M_t, eval split-path == combined-path bit-exact). 30-gate smoke suite green (config round-trip, generator modes/partial/clean-frame preservation, dual-lens grad→both heads+gate, B→M_suff & D→M_nec routing, eval back-compat, ASCII cp1252). 4 parallel notebooks isolate each axis: **A**=anchor (bottleneck reverted = proven floor), **B**=dual-lens, **C**=generator, **D**=all-in. Detection-protective: A/B/D regime byte-intact, surplus + detection-selected checkpoint. | 0.979 (A) | 0.546 (B) | 0.643 fake (A) | 0.416 (A) | noisy | **verdict in — anchor still best; dual-lens 2 narrow wins; generator a NET LOSS (run 6-16-26 2300hrs, EXP1=A/EXP2=B/EXP3=C/EXP4=D)** |
| | | | **P35 OUTCOME detail.** Detection: A 0.979 / B 0.977 / **C 0.955 / D 0.957** — warp/color generator modes DROPPED detection ~2.4pt and ONLY C/D show a val≫test gap (val 0.989 vs test 0.955 = overfit/mismatch). Losses converge cleanly all 4 (train_cls monotone; C/D end highest cls loss = generator made detection harder). Explanation, FAKE-ONLY (the robust read; gain-over-random is unreliable when calibration shifts): insertion A **0.643** / B 0.355 / C 0.503 / D 0.600 (ANCHOR most sufficient; B's 5-cell suff_topk REGRESSED it = P34 keep5 redux); deletion A 0.317 / B **0.281** / C 0.341 / D 0.511 (B most necessary); faith A **0.416** / B 0.360 / C 0.372 / D 0.286 (anchor best, new techniques all lower it). **TRAP caught:** B's `ins_gain` −0.032 looks best-ever but is a CALIBRATION ARTIFACT — its random baseline collapsed 0.591→0.338 (fake_acc@0.5 0.702, optimal thr 0.184); fake-only insertion proves B WORSE than anchor. Insertion NOT cracked. **lens_gate barely moved** (B 0.500→0.517, D 0.500→0.518) = classifier used both pools ~50/50 → evidence split in half → each map individually LESS necessary/faithful than the anchor's single map. suff_kept_mass saturated to 1.0 by epoch 2. Cross-dataset (want ≥0.75 all 6): only **Deepfakes-under-B crosses (0.853**, up from 0.764 anchor); B best cross-avg 0.628, **C WORST 0.566** (generator backfired on its OWN axis); FaceSwap 0.41–0.47 and Celeb-DF 0.51–0.55 at/below chance everywhere. K-drops uninformative on holistic HiDF in all 4 (per-frame drops ~0.1%, ratios noise; partial-fakes bought nothing because eval is holistic). **Genuine P35 wins: necessity-on-fakes (deletion 0.281) + Deepfakes cross (0.853), both dual-lens, both narrow.** Everything else no-improvement or regression. → Phase 36 intrinsic multi-layer decomposition. | | | | | | |
| 36 | 2026-06-17 | c5f38f3 | **Intrinsic multi-layer evidence DECOMPOSITION** (user-directed: the explanation MUST be intrinsic, NOT post-hoc; "layer by layer, each layer = X% of the evidence — multi, not single"). P35 proved a single (or dual) compact map cannot be both necessary AND sufficient on HOLISTIC fakes because the whole face is regenerated → the evidence is DISTRIBUTED. Fix: the model emits **L=4 COMPLEMENTARY attention maps (layers)** + a learnable **CONVEX contribution weight per layer**, all inside the forward pass; the combined (mixture) map **REPLACES M_t downstream** so the decomposition is faithful BY CONSTRUCTION and every existing metric (insertion / deletion / faithfulness / heatmaps) scores it with zero eval changes. Reads out as "layer k = X% of the evidence" (`[P36]` per-epoch log). One insertion point in `models/HiDF_eahn.py::_decompose` — rebinds `M_t_use` right after the bidirectional refine, so pooling, `M_t_up`, the B-pass sufficiency map (M_suff aliases M_t when dual off), the returned M_t, and ALL eval flow through it automatically. Two modes: **parallel** (EXP-A — L independent score heads + a diversity loss = pairwise map-overlap, pushing complementary regions) / **sequential** (EXP-B — 1 head applied L times with suppression `R←R·(1−M_k)` between steps, peeling evidence layer by layer = the progressive form). Per-layer convex weights from each layer's pooled feature. **Detection-protective**: cold-start gate `--decomp_gate_init −0.5` (sigmoid 0.378 → 62% of the PROVEN single map at init, both convex → convex pool); `spatial_band_hi 10→30` so the multi-region combined map can spread without the band fighting it. Built on the P35 anchor (best model 0.979; sbi on, dual/spatial_topk OFF). Flags `--decomp_enabled --decomp_mode --decomp_layers --lambda_decomp_div --decomp_gate_init`; OFF = byte-identical P33/34/35 (heads never built, M_layers None). Smoke (CPU, both modes): off-switch inert; M_layers (B,T,L,N) + layer_weights (B,T,L) convex sum-to-1; combined map convex; gate init 0.378; grad reaches decomp_heads / weight / gate / log_tau AND early_attn (detection path intact); #heads = L (parallel) / 1 (sequential); L respected — all green. New `[Phase36]/[DIAG-P36]/[P36]` prints (gate, overlap, per-layer % shares). 2-account program: **A**=parallel, **B**=sequential. | 0.968 (A) | 0.460 (A) | 0.537 fake (A) | 0.387 (A) | noisy | **mechanism WORKS (parallel); no metric beat anchor; parallel overran 12h (run 6-17-26 1500hrs, EXP1=A parallel / EXP2=B sequential)** |
| | | | **P36 OUTCOME detail.** Decomposition MECHANISM proven in **parallel (A)**: gate opened 0.378->0.408 (decomp = 41% of the pooling map), overlap stayed ~0.05 (layers complementary), shares differentiated from uniform 25/25/25/25 to **L1=8 L2=39 L3=5 L4=48%** = the intrinsic "X% here, Y% there" split, end-to-end. **Sequential (B) DEGENERATED**: shares stuck ~25% each, overlap blew up 0.02->0.46 (suppression failed under e2e training = 4 redundant maps). But NO metric beat the P35 anchor: detection A 0.968 / B 0.960 (anchor 0.979, -1..2pt); faith A 0.387 / B 0.375 (anchor 0.416); insertion fake-only A 0.537 / B 0.359 (anchor 0.643); deletion fake-only A 0.412 / B **0.248** (B best necessity). Cross-dataset still 1/6 >=0.75 (B Deepfakes 0.773; B best cross-avg 0.604 > anchor 0.594; A worst 0.564); FaceSwap/Celeb-DF at chance. Both converge cleanly (val AUC A->0.971@e9, B->0.963@e7). Irony: A "works" as a decomposition but generalizes worst; B "fails" as a decomposition but transfers best (broad overlapping map). Root cause of the flat metrics: the L layers still collapse to ONE combined mask for scoring + the gate is only 41% (single map still 59%) -> same single-map wall. Gaps: per-sample strips still report a single region (layers live only in `[P36]` logs); the causal layer-ablation was never run. **A parallel = go-forward base; B sequential retired.** Parallel hit the 12h cap (10 ep ~12 min/ep). -> Phase 37. | | | | | |
| 37 | 2026-06-17 | (pending) | **Two parallel tracks: finish the explanation win + a real cross-dataset attempt.** **Track 1 (`Exp_A_p36_parallel`, 9 epochs)** = turn the P36 parallel decomposition into a VALIDATED, publishable artifact (no mechanism change): (a) per-sample **layer figure** `*_layers.png` + `*_decomposition.txt` ("Layer k = X% -> region", `utils/HiDF_visualization.py::save_layer_decomposition_strip`, rendered from the forward-pass M_layers/layer_weights = intrinsic, not post-hoc); (b) **layer-ablation causal check** (`metrics/HiDF_explanation.py::layer_ablation_causal`, wired into the explanation suite -> `explanation_metrics.json["layer_ablation"]`): `model(frames, ablate_layer=k)` zeroes layer k's convex weight + renormalises, measures the fake-confidence drop, reports per-layer declared-share, drop-when-removed, declared%<->drop rank-corr, and top-share==top-drop rate (intrinsic-safe -- probes the model's OWN declared parts). Model gains optional `forward(ablate_layer=)` / `_decompose(ablate_layer=)` (None = byte-identical). 9 epochs (P36 best ckpt was e9; keeps <12h). **Track 2 (`Exp_C_p37_generalization`, from the P35 anchor, decomposition OFF)** = ONE axis, make the SBI self-blend look like a real swap so the cue transfers off-dataset: new `--sbi_freq_mismatch 0.5` (per-clip downscale->upscale of the blend SOURCE -> a resolution/frequency seam, the Face X-ray cross-dataset cue our warp+colour source lacked; `data/HiDF_self_blend.py`, 0.0 = byte-identical) + dosage bump `--sbi_stride 8->4` and `--lambda_sbi_cls 0.5->1.0` (the cue was under-dosed). Detection-protected: SBI is an auxiliary pass, the main A/B/D detection passes are untouched. Smoke: 7 gates (parallel + sequential forward, ablate zeroes+renorms the layer, causal dict shape, layer strip png+txt written, sbi freq off=byte-identical / on=differs, off-switch inert with ablate ignored) -- all green; full py_compile of all 8 edited files. | 0.961 (T1) | 0.414 (T1) / 0.512 (T2) | 0.605 fake (T1) | 0.275 (T1) | noisy | **ran (6-19-26 1600hrs, EXP1=T1 parallel / EXP2=T2 freq) -- figure+causal check landed; cross-dataset still not cracked; the causal check EXPOSED the real wall (see detail row)** |
| | | | **P37 OUTCOME detail.** **T1 (parallel decomp, EXP1):** detection 0.961, faith 0.275, insertion fake 0.605, deletion fake 0.407; the `*_layers.png` figures + `*_decomposition.txt` were written (per-sample shows spread layers e.g. fake 38/23/20/18%) and the **layer-ablation causal check RAN** -- and it is the most important result of the run: declared shares were **83/4.5/7.5/4.8%** but removing the 83%-share layer dropped fake-confidence only **3.6%** (others ~0), share->drop rank-corr +0.30, top-share==top-drop 1.0. So the declared %s are directionally real (top=top) but NOT proportional -- removing the model's own "main reason" barely moves it = the evidence is HOLOGRAPHICALLY REDUNDANT inside the model (every pathway carries a copy). This is the cleanest proof yet of why no compact map / insertion / k-drop moves. **T2 (freq cross-dataset, EXP2):** detection HELD 0.980, Deepfakes cross 0.72->**0.812**, Celeb-DF 0.41->**0.512**, Face2Face ->0.570 -- modest lifts, but FaceSwap still 0.383 and overall cross-avg ~0.587 (NOT cracked; deepfake cross-dataset is a known-hard open problem). **Diagnosis that drove P38:** the del/ins curves are ALSO confounded -- a blurred clip reads as MORE fake (blurred_conf ~0.82), so the blur deletion curve is non-monotonic/uninterpretable regardless of the map (a KNOWN metric flaw, fixed by ROAD). -> Phase 38. | | | | | |
| 38 | 2026-06-20 | (pending) | **ROAD debiased faithfulness (eval-only, the guaranteed win) + 3 parallel experiments.** After reading both 6-19 experiments end-to-end + the metric/loss/model code + the literature, root-caused the stuck explanation metrics to TWO fixable causes and one open problem. **(1) Metric confound (eval-only fix, zero detection risk, works on EXISTING checkpoints):** the blur/zero/mean del-ins fill pushes clips OFF-manifold and a blurred clip reads as MORE fake, so the curve can't be monotonic (Rong et al. ICML-2022 ROAD). New `metrics/HiDF_explanation.py::road_faithfulness` -- MoRF deletion with NOISY imputation (per-sample mean + matched Gaussian noise restores high-freq so the fill is near-manifold), run for THREE orderings (intrinsic map / gradient / random) so the curve is monotonic and the map is benchmarked against a faithful reference + the random floor. Plus `layer_ablation_cumulative` (remove top-1 layer -> drop, top-1+2 -> drop more, ... = the relative/layered readout the user kept asking for) and `forward(ablate_layer=[list])` (model now accepts a list). Wired into `run_explanation_suite` -> `explanation_metrics.json["road"]` + `["layer_ablation_cumulative"]`. **(2) Architecture (the steep-curve attempt):** new `--early_topk_frac` -- a HARD top-k binary gate on the conv feature map BEFORE the temporal transformer (vs P34 `spatial_topk_frac` which acted AFTER it on globally-mixed tokens, so input-pixel deletion couldn't track the map); STE backward; 0.0 = byte-identical. **(3) Cross-dataset** stays best-effort via the freq cue. **3 single-axis notebooks (9 ep, <12h each, run in parallel):** `Exp_A_p38_bottleneck` (early_topk 0.5, decomp OFF -- the gamble), `Exp_B_p38_decomp` (proven parallel decomposition + the new ROAD/cumulative eval -- the layered deliverable + safe baseline), `Exp_C_p38_frequency` (sbi_freq_mismatch 0.7 + modes blend,warp,color -- accuracy). ALL three auto-run ROAD so every run yields the honest relative curve. Smoke: stub-model ROAD (intrinsic AUC<random, monotonic drop, fully-removed<baseline) + cumulative monotonic + real-EAHN early_topk (OFF inert, ON differs, grad->early_attn via STE, ablate_layer=[list]) -- all green; py_compile all 5 edited files. | 0.972 (C) | 0.545 (C) | 0.475 fake (C) | 0.379 (C) | noisy | **ran (6-20-26 1400hrs, EXP1=A bottleneck / EXP2=B decomp / EXP3=C freq) -- NO target metric beat anchor; ROAD EXPOSED the fill is STILL confounded + the bottleneck failed (see detail row)** |
| | | | **P38 OUTCOME detail.** Detection ALL below the 0.979 anchor: A bottleneck **0.945** (gamble cost detection AND ins_gain stayed -0.169 = failed its purpose), B decomp 0.949, C freq **0.972** (best, closest to anchor; fake_acc 0.855). **ROAD verdict -- the fill is STILL confounded on 2/3:** `fully_removed_conf` A **0.776** / B **0.822** >> baseline 0.386 / 0.364 (noise reads as MORE fake, same disease as blur), so their "intrinsic AUC < random" gain is a SPATIAL-COMPACTNESS artifact (concentrated noise keeps the rest of the frame clean longer), NOT faithfulness -- do not trust. ONLY **C (freq) had a clean fill** (`fully_removed` 0.404 ~= baseline 0.438): there random removal is FLAT (AUC 0.422) while the map causes a big hump UP (intrinsic 0.628) = the map localizes sensitive regions BUT the model answers ANY local perturbation with "more fake" (curve rises +0.31 then returns) -- the clean measurement of perturbation=fake. **Cumulative layer-ablation (B) IS monotonic** (remove top-1 layer 34% share -> 0.2% drop; top-3 layers 92% share -> **2.9% drop**) = the relative "remove more -> drop more" readout the user wanted, BUT the magnitude reconfirms HOLOGRAPHIC REDUNDANCY (removing 92% of declared evidence barely moves the logit). Cross-dataset best Deepfakes 0.81 (A) / 0.80 (C); FaceSwap 0.41-0.49, Celeb-DF 0.46-0.56 (NOT cracked). ROOT CAUSE now proven 3 ways (P37 ablation 83%->3.6%; P38 cumulative 92%->2.9%; P38 ROAD perturbation=fake) = ARCHITECTURAL: the prediction pools POST-transformer GLOBALLY-MIXED tokens Q, so M_t weights global summaries. -> Phase 39 additive head. | | | | | |
| 39 | 2026-06-20 | (pending) | **Additive local-evidence head -- the FAITHFUL-BY-CONSTRUCTION fix** (user-directed: "why the f*** are faithfulness/insertion/deletion/k-drops not fixed -- I want them fixed"). Root cause is now proven 3 independent ways to be ARCHITECTURAL, not a missing loss: the prediction pools POST-transformer tokens Q (`models/HiDF_eahn.py` forward ~L620), and the transformer has a GLOBAL receptive field, so each Q[n] is a global summary -> M_t weights global summaries -> removing any region changes nothing (the ~15 phases of faith/ins/del losses can't win because every bottleneck pass re-globalizes through the same backbone, and `loss_faith = match(full, bottlenecked)` literally REWARDS redundancy). **Fix (`--aeh_enabled`):** a parallel head scores PRE-transformer LOCAL features per cell `e[t,n]=MLP(feats_5d[t,n])` and forms the logit as `scale * sum_t M_frame[t] * sum_n M_t[t,n] * e[t,n] + bias`, so each cell's contribution is EXACTLY `M_frame*M_t*e` and deletion/insertion/k-drop/faithfulness move BY CONSTRUCTION. The forward REBINDS `M_t_up` to the contribution map `M_t*e` (the faithful saliency), so every eval ordering metric scores it with ZERO eval changes. **Detection-protective:** a blend `logit=(1-g)*base+g*aeh` with `g` ramping 0->`aeh_gamma_max` over `aeh_warmup_epochs=2` (registered buffer set per-epoch by the train loop, read at eval), plus an aux focal-cls on `aeh_logit` (`--lambda_aeh_aux 0.5`) so the head trains from epoch 1; at g=1 the faithful head IS the sole predictor (explanation is not decoration). OFF (default) = no params/buffer, byte-identical P33-38. Single Phase-39 axis. Smoke (CPU): OFF no-buffer/no-fields; ON aeh_logit shape, g=1 logit==aeh_logit, linear blend (g=0.5 == mean), saliency==M_t*e and DIFFERS from raw M_t, grad->aeh_score+aeh_scale, buffer-driven eval path, decomp+aeh coexist -- all green; py_compile all 3 edited files. 2 parallel notebooks: **A** `Exp_A_p39_additive_g100` (gamma_max 1.0 = 100% faithful headline) / **B** `Exp_B_p39_additive_g80` (gamma_max 0.8 = detection-protected hedge). | 0.982 (B) | 0.580 (B) | 0.663 fake (B) | del 0.229 (B) | ROAD intrinsic>rand+grad | **RAN (6-21-26 1500hrs, EXP1=A g100 / EXP2=B g80) -- FIRST EXPLANATION WIN in ~15 phases; both crashed only at the final cosmetic heatmap-strip (signed saliency->NaN); fixed in P39.1 (see detail rows)** |
| | | | **P39 OUTCOME detail -- the metrics MOVED.** First time deletion/insertion/ROAD beat their controls after ~15 phases. **g80 (EXP2, the winner) is BETTER than g100 (EXP1) on everything** -> the 20pct base-head admixture stabilises training so the additive head is cleaner. **Detection:** g80 **0.982** (>= 0.979 anchor; fake_acc 0.888@0.5) / g100 0.967 (fake_acc 0.645@0.5, balanced 0.914@opt-thr 0.235). **Deletion** (lower=better): g80 0.229 / g100 0.198, both << random 0.468/0.430 -> del_gain **+0.239 / +0.232** (intrinsic ordering deletes far better than random). **Insertion** (higher=better): g80 **0.580** >> random 0.464 = ins_gain **+0.115** (POSITIVE at last; ins_fake 0.663); g100 0.426 ~ random (ins_gain -0.004, flat -- the pure additive head trained worse). **Debiased ROAD (MoRF, intrinsic vs gradient vs random) -- the headline:** intrinsic BEAT random AND gradient on BOTH runs: g80 intrinsic AUC 0.357 < gradient 0.457 < random 0.430 (note intrinsic best), **drop@10pct +0.227 drop@50pct +0.159** (remove top-10pct of cells -> fake-conf falls 0.227 = the "delete 10pct -> >=10pct drop" the user demanded); g100 intrinsic AUC 0.311 < random 0.404, drop@10pct +0.092. This REVERSES P38 where removal read as MORE fake (fully_removed 0.78/0.82). **Still lagging:** faithfulness_corr negative (-0.084/-0.119) -- it correlated a SIGNED saliency vs gradient-MAGNITUDE (a bug, fixed in P39.1); k-drops still noisy (holistic eval has no temporal localisation to find); cross-dataset unchanged (Deepfakes 0.76-0.78, Celeb-DF 0.51-0.54, FaceSwap 0.44-0.46 = the separate open problem). **The crash:** `_peakiness`/`_entropy` did `log()` on the signed AEH map -> NaN -> `int(NaN*20)` in `save_annotated_frame_strip`; happened AFTER ALL metrics + cross-dataset + 50 XAI overlays saved (only the 5 strip videos were lost). -> P39.1. | | | | | |
| 39.1 | 2026-06-21 | (pending) | **Crash fix + saliency made non-negative (eval/viz-only, ZERO training/detection risk).** (1) `models/HiDF_eahn.py`: the eval-facing AEH saliency `aeh_sal_btn` is now `F.relu(M_t*e)` (the POSITIVE = fake-evidence part); the LOGIT keeps the SIGNED sum (`_frame_sum` computed before the ReLU) so detection is byte-identical, and ReLU preserves the top-cell ordering deletion/insertion/ROAD rank by. This makes M_t_up non-negative -> entropy/peakiness well-defined AND faithfulness_corr becomes magnitude-vs-magnitude (expected to flip POSITIVE). (2) `scripts/HiDF_evaluate.py` `_entropy`/`_peakiness`: abs + nan_to_num + zero/NaN-sum guard (defense-in-depth: no saliency can ever crash a run again). (3) `utils/HiDF_visualization.py`: clamp NaN/out-of-range attention scores before `int(score*20)`. Unit-tested on signed/zero/NaN maps (all finite, no crash). **Next run = confirm + bracket:** `Exp_B_p39_additive_g80` (CONFIRM the winner, clean complete artifacts + real entropy + positive faith_corr) and `Exp_A_p39_additive_g60` (gamma_max 0.6 = LOW bracket of the gamma ablation; with g100=1.0 in hand and g80=0.8, tests whether 0.8 is a true peak). g100 notebook retired (superseded; numbers retained in 6-21 results). | 0.984 (g60) / 0.982 (g80) | 0.544 / 0.539 | 0.505 (g60) / 0.564 (g80; fake 0.704) | **+0.299 / +0.319 (FLIPPED POSITIVE)** | 10.8x (g60) / neg (g80) | **RAN 6-22-26 0800hrs (EXP1=g60 / EXP2=g80) -- crash FIXED (run completed, real entropy), faith_corr flipped POSITIVE as predicted; insertion still capped ~0.5-0.56 by DIFFUSE evidence -> Phase 40 (see detail row)** |
| | | | **P39.1 OUTCOME detail -- the gamma ablation (g60 vs g80) + the concentration diagnosis.** Crash fix CONFIRMED: both ran to completion, entropy is a real number, no NaN. **faithfulness_corr flipped POSITIVE** (+0.299 g60 / +0.319 g80) exactly as the ReLU-saliency fix predicted. **Detection rock-solid:** g60 **0.984** / g80 **0.982** (both >= 0.979 anchor). **Deletion beats random on both** (g60 del_gain +0.289 / g80 +0.148). **ROAD (lower AUC=more faithful):** g60 intrinsic AUC **0.641 < random 0.747 AND < gradient 0.792** = intrinsic map beats BOTH (gain_vs_rand +0.106, the clean ROAD win); g80 intrinsic 0.566 ~ random 0.558 (tie) but beats gradient; **drop@10pct fake +0.166 (g60) / +0.171 (g80)** = "delete 10pct -> ~17pt fake-conf drop". **Insertion (THE gap):** all-sample 0.505 (g60, ins_gain -0.101 LOSES to random) / 0.564 (g80, ins_gain +0.083 beats random); **fake-only g80 = 0.704 (already >=0.7 on fakes)** but the user wants the headline >=0.7. **k-drops:** g60 clean ~10x ratio (but tiny 0.006 magnitude); g80 sign-flipped negative (temporally redundant, weakest axis). **ROOT CAUSE of the insertion cap = DIFFUSE evidence:** peak_mode_share **0.14** (top cell holds 14pct of mass), m_t_std_mean 0.013-0.017 (~uniform over 49 cells) -> no small patch is SUFFICIENT. Now FIXABLE because the additive head makes the logit literally = sum M_t*e. Run-to-run variance ~0.1 (don't over-read <0.05 diffs). Cross-dataset unchanged (Deepfakes 0.78-0.80, Celeb-DF 0.54). -> Phase 40 CONCENTRATION. | | | | | |
| 40 | 2026-06-23 | (pending) | **Evidence CONCENTRATION on the additive head -- close the insertion gap (3 experiments).** P39 made deletion/ROAD causal but the learned per-cell evidence is DIFFUSE (peak_mode_share 0.14) so insertion stalls ~0.5. Three single-axis attempts to concentrate the fake-evidence into a few cells so they are both NECESSARY (deletion) and SUFFICIENT (insertion), all detection-protected at gamma=0.8 + aux focal-cls, all default-OFF=byte-identical Phase 39. **(EXP-1) `--aeh_topk_frac 0.15`:** the additive logit is formed from ONLY the top-k (~7 of 49) most fake-positive cells per frame via a HARD top-k mask with a straight-through gradient (forward=top-k sum, backward=dense). Forces <=k cells SUFFICIENT by construction -> insertion rises. **(EXP-2) concentration losses (no bottleneck):** `--lambda_aeh_concentrate 0.05` (entropy penalty on normalised ReLU(M_t*e), FAKE clips only) + `--aeh_suff_topk_frac 0.15 --lambda_aeh_suff 0.3` (focal-cls that the top-15pct partial logit already crosses the margin = trains insertion directly). Detection-safe hedge. **(EXP-3, 7 epochs) `--decomp_enabled` (parallel,4) + `--aeh_topk_frac 0.15`:** re-enables the per-layer decomposition so the suite fills `layer_ablation`+`layer_ablation_cumulative` (the "delete one layer->drop, the next->drop more" readout) ON the additive head -- expected MONOTONIC + declared share predicts causal drop (holographic redundancy BROKEN); carries EXP-1's concentration so it doubles as a candidate FINAL model + the layered figure. New: `aeh_contrib`/`aeh_topk_logit` exposed in `EAHNOutput`; `[DIAG-P39/40]` logs `top_cell_share` (watch climb above 0.14). Smoke (CPU): OFF parity, EXP-1 top-k IDENTITY (logit==scale*sum M_frame*sum_topk(M_t*e)+bias) + STE dense backward, EXP-2 concentration+sufficiency losses finite & backprop to aeh_score, EXP-3 aeh+decomp coexist, default knobs OFF==Phase 39 -- all green; CLI flags parse. 3 notebooks: `Exp_A_p40_topk_g80` / `Exp_B_p40_concentrate_g80` / `Exp_C_p40_consolidate_g80`. | 0.976 (E1) | 0.544 (E1) | 0.615 (E1; fake 0.729) | 0.142 (E1) | noisy | **ran 6-23-26 1600hrs (EXP1=topk / EXP2=concentrate) -- EXP1 BACKFIRED, EXP2 never ran its flags -> Phase 41 (see detail row)** |
| | | | **P40 OUTCOME detail -- one lever backfired, the other never fired.** **EXP-1 hard top-k STE (`--aeh_topk_frac 0.15`) ran correctly and BACKFIRED:** the dense straight-through backward let the model EQUALISE cells to game the top-k mask, so evidence got MORE diffuse (peak_mode_share 0.14->**0.066**, m_t_std 0.0157), deletion 0.33->**0.519** (WORSE), faithfulness 0.319->**0.142** (WORSE), ins_gain_over_random -0.009 (still loses to random); insertion_fake 0.729 looked up but deletion_fake 0.592 confirms it's a baseline shift, not localisation. Detection held 0.976. Hard bottleneck on the FORWARD is the wrong tool. **EXP-2 concentration NEVER EXERCISED its flags:** the launched notebook ran the pre-P40 g80 command (log shows NO `--lambda_aeh_concentrate/--lambda_aeh_suff/--aeh_suff_topk_frac`, NO `[Phase40]` line, git history only up to 87256e6) so every metric is BIT-IDENTICAL to the 6-22 g80 baseline (faith 0.3193408, ins 0.5638368, peak 0.1411339, det 0.9819) -- concentration was never tested. **EXP-3 not run** (depended on E1/E2). Net: zero real progress, but the hard-topk approach is eliminated and concentration remains the untested lever. -> Phase 41 re-implements concentration as a CORRECTLY-WIRED pure-aux loss + verifies the flags reach the config and print in the log. | | | | | |
| 41 | 2026-06-23 | (pending) | **Soft evidence concentration as PURE AUX losses -- the correctly-wired insertion fix** (user-directed: "things are not moving... GET ME RESULTS, low deletion, high insertion, perfect faithfulness, accurate K drops"). After reading both 6-23 runs end-to-end + every metric's exact computation (`metrics/HiDF_explanation.py` + `scripts/HiDF_run_explanation_suite.py`): the eval saliency for ALL metrics is `ReLU(M_t*e)` (spatial) / `M_frame` (temporal), so all four targets are governed by ONE quantity -- the CONCENTRATION of those distributions on fakes. P40 EXP-1 backfired (hard STE on the forward -> model equalises cells); P40 EXP-2's concentration never ran. **Fix:** concentrate via aux losses that NEVER touch the dense gamma-blend prediction (detection-safe) and CANNOT be gamed flat (a uniform map gives the MAXIMUM loss). New `--lambda_aeh_topk_mass` (+`--aeh_mass_topk_frac 0.15`): maximise the share of the top ~7/49 cells in the normalised ReLU(M_t*e), FAKE clips only -> few cells SUFFICIENT (insertion up) + NECESSARY (deletion down). New `--lambda_aeh_temporal_conc`: minimise M_frame entropy over frames, fake-only -> a few frames carry evidence (sharper k-drop). Hard top-k (`--aeh_topk_frac`) DROPPED in the notebooks. All default 0.0 = byte-identical P39/40. Smoke (CPU): OFF guarded zero, ON fires + backprops to params, all-real -> fake-gated losses exactly 0, concentrated contrib lowers mass-loss vs uniform (objective points right + uniform=MAX), default lambdas 0 -> P40 identical -- all 5 green; CLI merge verified (flags reach config, omit -> 0.0, hard topk stays off). 2 notebooks: **A** `Exp_A_p41_concentrate_strong_g80` (topk_mass 0.5 + temporal 0.1 + soft suff 0.3 = results-maximising) / **B** `Exp_B_p41_concentrate_clean_g80` (topk_mass 0.3 + temporal 0.05 ONLY = clean attribution). Both gamma=0.8, 9 ep, run in parallel. | 0.974 (B) / 0.920 (A) | 0.524 (B) | 0.498 (B; fake 0.626) | **0.464 (B, record)** | noisy | **ran 6-24-26 1000hrs (EXP1=A strong / EXP2=B clean) -- lever FIRED this time but concentration trades insertion for faith, and EXP-A broke detection; 3rd independent proof concentration is the WRONG lever -> Phase 42 (see detail row)** |
| | | | **P41 OUTCOME detail -- concentration is dead (third confirmation); EXP-A broke detection.** The flags FIRED this time (EXP-A log has the `[Phase41]` banner + `[DIAG-P39/40/41] L_aeh_mass=0.466 top7_share=0.535`), unlike P40 EXP-2 -- so this is a real, trustworthy result. **EXP-A (strong: mass 0.5 + temporal 0.1 + suff 0.3):** detection **0.982 -> 0.920** (the strong aux losses damaged the detector; fake_acc@0.5 0.679), deletion 0.329 -> **0.524** (WORSE), insertion 0.564 -> **0.515** / fake-only 0.704 -> **0.595** (WORSE), faith 0.319 -> 0.360, peak_mode_share 0.141 -> 0.180. ROAD intrinsic 0.440 < gradient 0.488 < random 0.510 (intrinsic still wins, but the run is disqualified by the detection drop). **EXP-B (clean: mass 0.3 + temporal 0.05):** detection HELD 0.974, faith **0.464 = best ever** (beats anchor 0.416), but deletion 0.555 / insertion 0.498 / fake-only **0.626** all WORSE than the no-concentration g80, and ROAD random 0.288 < intrinsic 0.335 (random wins = bad). **The smoking gun: `m_t_std_mean = 0.0034` (M_t stayed UNIFORM).** The mass loss is satisfied through the evidence embedding `e`, never through M_t, so the displayed saliency concentrates (peak up) while the prediction keeps integrating diffusely -> map<->logit alignment (deletion/insertion) DEGRADES. Pattern across P40+P41: every concentration attempt lowers insertion (0.704 -> 0.595/0.626); the BEST insertion came from g80 with NO concentration. Concentration trades insertion for faithfulness -- not a win. Also note: highest-detection run had highest insertion (insertion is bounded by fake-confidence), and fake_acc@0.5 is genuinely poor (0.68-0.74). Cross-dataset unchanged (Deepfakes ~0.75, Celeb-DF ~0.52). -> Phase 42 STOPS concentrating: reframe eval (confound-free) + fix fake-confidence. | | | | | |
| 42 | 2026-06-24 | (pending) | **Stop concentrating; reframe the eval + fix fake-confidence** (user-directed: "results are miserable again... I need explanation metrics + fake accuracy + cross-dataset"). P41 closed concentration (3rd proof: hard top-k backfired, soft mass made insertion worse, the model games any concentration loss through `e` while M_t stays uniform at std 0.003). Two data-driven facts redirect the work: (1) the blur del/ins baseline is CONFOUNDED -- a blurred clip reads as MORE fake, so the insertion curve is floored/non-monotonic (`ins@10% 0.559 > ins@50% 0.540`); the confound-free metric is ROAD (Rong ICML-2022), where the intrinsic map already BEATS random AND gradient. (2) insertion is bounded by full-clip fake confidence, and fake_acc@0.5 is poor (0.68-0.74, scores cluster low, optimal thr ~0.25-0.39); the highest-detection run had the highest insertion. **Fix A (eval, zero training risk):** `metrics` already supports blur/black/mean fills -- new `--insertion_baselines blur,black,mean` reports del/ins under ALL THREE in one pass (`scripts/HiDF_run_explanation_suite.py` refactored into a per-baseline helper; headline keys = first baseline for back-compat; new `result["del_ins_baselines"]`); ROAD stays the headline faithfulness metric. **Fix B (1 training knob):** raise `--focal_alpha_pos 1.0 -> 1.5` (REAL weight 0.5 unchanged) to push fake recall/confidence -- targets the explicit fake-accuracy ask AND lifts the insertion ceiling. **Enabler:** scheduler `T_max=max(epochs,1)` so an eval-only run (`--epochs 0 --resume_checkpoint <best.pth>`) reframes an EXISTING checkpoint in ~1h (no 12h retrain). Smoke (CPU): blur/black/mean give distinct finite [0,1] del/ins (black 0.127 vs blur 0.526 = arg flows through), baseline-list parse+dedup, `--insertion_baselines` reaches config (omit -> "blur"), `--focal_alpha_pos 1.5` reaches config, epochs=0 scheduler constructs + empty training range -- all green; py_compile all 3 edited files. 2 notebooks: **A** `Exp_A_p42_clean_rebalance_g80` (clean g80 additive head, NO concentration, focal_alpha_pos 1.5, multi-baseline eval, 9 ep = the genuine fix) / **B** `Exp_B_p42_evalonly_reframe` (epochs 0 + resume an uploaded checkpoint -> multi-baseline + ROAD table in ~1h = the fast banked result). | TBD | TBD | TBD | TBD | TBD | **A (`Exp_A_p42_clean_rebalance_g80`) RUNNING 6-24-26; B eval-only NOT pursued -- user redirected away from reframing weak checkpoints toward a model that meets ALL criteria first -> Phase 43** |
| 43 | 2026-06-24 | (pending) | **Local generic forgery cue -- attack cross-dataset + explanation together** (user-directed: "showcase the RIGHT results, not just results; plan experiments; results come once we have a model that meets explanation + cross-dataset fake/real + in-dist criteria"). Root cause, grounded in the 6-24 eval: the detector keys on a HOLISTIC, HiDF-specific RGB fingerprint -- cross-dataset FAKE accuracy is 0.23 (Deepfakes) / **0.06 (Celeb-DF)** (calls off-dataset fakes "real"; real_acc stays ~0.95 only by defaulting to real), and the SAME holistic fingerprint is why explanation insertion/heatmaps stay diffuse (no compact region is sufficient). One disease, two symptoms; no frequency branch exists in the model (grep-confirmed). Fix = make detection rely on LOCAL GENERIC artifacts (spectral + blend seams), which transfer across manipulation types AND localize. Two single-axis experiments, both on the running EXP-1 g80 + focal_alpha_pos 1.5 + multi-baseline base (so all three are comparable), NO concentration: **EXP-2 `Exp_B_p43_freq_local_g80`** = `--aeh_freq_enabled`: a fixed 3-filter SRM high-pass branch -> small CNN -> 7x7 spectral features concatenated into the additive head's per-cell evidence `e` (`models/HiDF_eahn.py` `_srm_kernels`/`_aeh_freq_features`; config `aeh_freq_enabled`/`aeh_freq_dim`; OFF=byte-identical P39, aeh_score reads d vs d+32). **EXP-3 `Exp_C_p43_sbi_primary_g80`** = self-blend-PRIMARY (config only): `--sbi_modes blend,warp,color --sbi_freq_mismatch 0.7 --lambda_sbi_cls 2.0 --sbi_stride 4` (Face X-ray / SBI generic-seam training, the established cross-dataset method). Smoke (CPU): SRM high-pass (each filter sums ~0), OFF aeh_score==d + no freq modules, ON aeh_score==d+32 + forward finite + saliency finite, backward grad reaches aeh_freq_cnn (load-bearing) AND aeh_score, SRM buffer fixed (no grad) -- all green; py_compile config+model. EXP-1 (running) is the control; EXP-2/3 isolate the two unifying levers; the combine (freq + sbi-primary) is the next run if both help. | 0.982 (E2) | 0.529 (E2) | 0.655 fake (E2) | 0.361 (E2) | noisy | **ran 6-25-26 0100hrs (EXP1 control / EXP2 freq / EXP3 sbi-primary) -- freq is the ONLY winning lever; cross-dataset fake recall STILL catastrophic; sbi-primary BACKFIRED (see detail row)** |
| | | | **P43 OUTCOME detail.** Both levers verified fired (EXP2 ckpt +126KB freq params; EXP3 banner sbi_modes=[blend,warp,color] sbi_cls=2.0 stride=4 freq_mismatch=0.7). **IN-DIST:** EXP2 (freq) BEST detector AUC **0.982** / bal 0.925 / fake@0.5 0.903 / real 0.947; EXP1 (control) 0.979 / fake 0.908; EXP3 (sbi-primary) 0.971 but fake@0.5 collapsed to **0.689** (real 0.989, optimal_thr 0.277 = REAL-biased -- over-specialised to the SBI seam). **CROSS-DATASET fake_acc@0.5 (real in parens) = still a "real detector" off-dist:** Deepfakes .31(.91)/**.44**(.92)/.22(.95); FaceShifter .08/.14/.08; FaceSwap .04/.02/.01 (~chance AUC .44-.50); Celeb-DF .13/.08/.10; Face2Face/NeuralTextures (EXP1) .07/.14. The **freq head is the ONLY lift** in cross fake recall (Deepfakes .31->.44, FaceShifter AUC .61->.68) AND best faith_corr 0.361 (vs 0.165 control) AND most sample-specific maps (inter_sample_cos 0.77->0.61). **EXPLANATION:** blur baseline CONFOUNDED (EXP1 del blur 0.355 but BLACK 0.609); under black/mean fake insertion ~0.72 (EXP1) = honest read; ROAD: EXP3 WINS clean (intrinsic 0.586 < grad 0.696 < rand 0.715, full AND fake) but EXP1/EXP2 LOSE ROAD to random. NO model wins all (EXP2 best detector+faith+cross-fake; EXP3 best ROAD+map-variety but insertion COLLAPSED 0.36 + real-biased). All 3 still print "near-identical maps across samples". **LOSSES:** train_cls converges clean (0.19->0.07); train_exp barely moves (3.13->2.95, -6% vs cls -63% = explanation objective SATURATED); train_faith RISES 0.012->0.071 (traded for cls); val fake/real_acc OSCILLATE at 0.5 (optimal_thr 0.31-0.71 = uncalibrated operating point; AUC stable 0.97-0.98). -> Phase 44. | | | | | |
| 44 | 2026-06-25 | (pending) | **Artifact-routed attention + multi-band freq + moderate-SBI + calibration -- 4 experiments (user-directed: "go with A, B, C1 AND C2; get results the way I want").** P43 verdict: the freq head is the only winning lever but it only fed the additive evidence `e`, so the DISPLAYED map M_t still keyed on RGB (insertion/ROAD weak, "near-identical maps"); sbi-primary backfired (real-biased); the operating point drifts. Four single-axis levers, all on the proven g80 + freq base, all default-OFF byte-identical (smoke-verified), comparable: **(A) artifact-routed attention** `--aeh_attn_freq` -- a ZERO-INIT residual proj adds a per-cell spectral score to the attention logits, so M_t (and thus the contribution map M_t*e the predictor sums over) points at the artifact -> insertion sufficiency + sample-specific heatmaps + cross-fake; cold-start identical (proj output 0) yet trainable from step 1 (grad depends on freq inputs, not the zeroed output -- the alpha-scalar version was caught starving the proj of gradient in smoke). **(B) freq + MODERATE SBI + calibration** `--sbi_modes blend,warp,color --sbi_freq_mismatch 0.5 --lambda_sbi_cls 1.0 --sbi_stride 6` (NOT primary -> keeps HiDF signal dominant) + `--lambda_calib 1.0` (pins batch mean-prob to the fake-rate so the 0.5 threshold stops drifting). **(C1) union** = A + B (candidate FINAL model). **(C2) multi-band front-end** `--aeh_freq_mode multiband` = 6-tap SRM+Laplacian+Sobel-x/-y bank (vs 3-tap SRM) feeding both e and M_t, + routing. Code: `models/HiDF_eahn.py` (`_aeh_freq_map` refactor, `_srm_kernels(mode)`, zero-init `aeh_attn_freq_proj`, forward routing), `scripts/HiDF_train_real.py` (calib regularizer + `[Phase44]` banner), `HiDF_config.py` (3 fields/args). Smoke (CPU, 34 gates): srm-mode + OFF byte-identical (no new params, aeh_score in=d/d+32), multiband buffer (6,3,5,5) + CNN in=6, routing zero-init = cold-start identity + grad reaches proj/CNN/score from step 1, every tap sums ~0, calib formula finite & 0 at calibration, all 4 notebook configs parse+map -- all green. 4 notebooks: `Exp_A_p44_artifact_attn_g80`, `Exp_B_p44_freq_sbi_calib_g80`, `Exp_C1_p44_union_g80`, `Exp_C2_p44_multiband_g80`; 9 ep each, gamma 0.8. P42/P43 notebooks archived. | 0.980 (E4) | 0.55 (E3) | weak (24-clip noise) | 0.36 (E2) | noisy | **ran 6-25-26 1400hrs (EXP1 A / EXP2 B / EXP3 C1 / EXP4 C2) -- calibration WON; attn_freq HURT; a STRUCTURAL MEASUREMENT FAULT found (see detail row) -> P45** |
| | | | **P44 OUTCOME detail.** All 4 banners fired; SBI ran (414 passes/ep). **IN-DIST:** EXP1 (attn_freq) WORST -- AUC 0.967, fake@0.5 **0.679**, opt_thr 0.281 (real-biased); EXP2 (calib) AUC 0.974, fake 0.837, opt_thr 0.415; **EXP3 (union+calib) best operating point in project history -- fake@0.5 0.903, real 0.929, opt_thr 0.442**, AUC 0.971; EXP4 (multiband) best AUC **0.980**, fake 0.837. **=> calibration (`lambda_calib`) is a clean win** (fixes the long-standing fake_acc ~0.68-0.74 complaint); attn_freq drops detection and never sharpened M_t (m_t_std 0.0095-0.013, near-uniform in ALL four). **CROSS-DATASET:** AUC unmoved by every lever (Deepfakes ~0.78, Face2Face ~0.55, FaceSwap ~0.40 *below chance*, Celeb-DF ~0.50); calib only shifted the threshold (EXP3 Deepfakes fake_acc .22->.49) at the cost of real_acc (.99->.84) -- ranking did NOT improve. **THE FAULT (root cause of "explanation never moves"):** the additive head's faithfulness is exact in CONTRIBUTION space (logit = scale*Sum M_frame*M_t*e + bias), but EVERY causal metric (deletion/insertion/ROAD/faith_corr) occludes INPUT PIXELS and re-runs the FULL blended `.prob` -- which re-globalizes (holistic redundancy) and carries a 20% immovable base term (eval gamma 0.8), and runs on only ~24 fakes (del_fake swung 0.46/0.75/0.69/0.40 across near-identical configs = noise). We built a faithful head and graded it with a ruler that can't see it. -> P45 measures it in contribution space + concentrates the SIGNED sum. | | | | | |
| 45 | 2026-06-25 | (pending) | **Measure the additive head in CONTRIBUTION space + signed-sum concentration (user: "OK to retrain if it pays off").** Two code changes on the proven base (calib + freq-into-e kept, attn_freq DROPPED). **(1) Contribution-space del/ins (eval-only, analytic):** the head's logit is an exact sum over cells, so deletion/insertion of a cell == removing/adding its term -- no forward, no pixel occlusion, no re-globalization, no 20%-base floor; runs on ALL fakes (kills the 24-clip noise). New `ExplanationMetrics.contribution_del_ins` (numpy, exact), reported on BOTH the pure-aeh logit (intrinsic faithfulness) and the blended decision logit; suite collects `aeh_contrib`/`M_frame`/`base_logit` in one pass. Model exposes `base_logit` (pre-aeh-blend). **(2) `--lambda_aeh_sigconc` (one new training axis):** per fake clip, push `s_topk/s_all -> 1` of the EFFECTIVE per-cell contribution `scale*M_frame*(M_t*e)` so the top ~7/49 cells carry the WHOLE logit (tail -> 0). P40/P41 concentrated ReLU(M_t*e) -- the positive-part DISPLAY -- which the model gamed via `e` while the SIGNED prediction stayed diffuse (m_t_std 0.003); this acts on the signed sum that enters the logit, so it CANNOT be gamed by equalising (uniform -> ratio k/N, the MIN) and is exactly what the new metric reads. Code: `HiDF_config.py` (2 fields/args), `models/HiDF_eahn.py` (`base_logit`), `scripts/HiDF_train_real.py` (sigconc loss + `[Phase45]` banner + `sig_capt` diag), `metrics/HiDF_explanation.py` (metric), `scripts/HiDF_run_explanation_suite.py` (collect+call+JSON). Smoke (CPU, 15 gates): metric DISCRIMINATES (concentrated ins 0.99 vs diffuse 0.89, del 0.53 vs 0.88, gain +0.10 vs +0.00), exact single-cell ins>>del, sigconc diffuse 0.85 > conc 0.00 and UNIFORM 0.857 (not gameable), base_logit reconstructs blended logit exactly, default-off byte-identical -- all green. 4 notebooks (8 ep, gamma 0.8): `Exp_A_p45_anchor_g80` (NO conc -- the measurement reference), `Exp_B_p45_sigconc_mod_g80` (0.15), `Exp_C_p45_sigconc_strong_g80` (0.4), `Exp_D_p45_sigconc_multiband_g80` (0.15 + 6-tap multiband). P44 notebooks archived. | 0.972 (E2) | 0.563 (E2) | 0.832 contrib-fake (E2) | 0.433 (E2) | n/a | **ran 6-26-26 1200hrs (E1 anchor / E2 mod / E3 strong / E4 multiband) -- WIN CONFIRMED: contribution-space metric fired on ALL 392 fakes; EXP2 is the publishable model (see detail row). The "all failed" was a REPORTING FAULT.** |
| | | | **P45 OUTCOME detail = THE EXPLANATION + DETECTION WIN, hidden by a reporting fault.** All 4 banners fired (`[Phase45]`, `sig_capt` moved); contribution-space del/ins ran on **392 fakes** (vs the old ~24-clip pixel noise). **The new metric reveals the head was faithful all along:** every experiment is faithful in contribution space (blended ins/del gains > 0 on fakes). **WINNER = EXP2 (`lambda_aeh_sigconc=0.15`):** detection AUC **0.972**, fake@0.5 **0.867**, real 0.954, bal **0.911** (best operating point of the project) + explanation insertion(fake) **0.832** / deletion 0.513 / ins_gain +0.132 / del_gain +0.186 (pure head 0.836/0.385/+0.189/+0.261). Sigconc ablation works as designed (E1->E3 deletion 0.471->0.404, head-del 0.388->0.328, gains rise) -- the FIRST working concentration lever (P40/P41 concentrated the ReLU display and HURT del/ins; this targets the signed sum and helps). **THE FAULT (why the user saw "all failed"):** `eval/report.txt` (HiDF_evaluate.py:757) stamps "Faithful? NO" from the OLD pixel-occlusion del/ins (ins 0.364 < del 0.470, confounded, ~24 fakes) while the correct contribution-space result (ins 0.832, faithful YES) sat unread at the bottom of the JSON. **FIX (no retrain):** (a) regenerated corrected reports + `P45_PUBLISHABLE_SUMMARY.md` for all 4 folders from saved JSON; (b) patched `HiDF_evaluate.py` (label the pixel verdict a "proxy" + point to authoritative file, additive-only) and `HiDF_run_explanation_suite.py` (write authoritative `faithfulness_report.txt` + `faithfulness_headline` in JSON). Cross-dataset still limited (FaceSwap ~0.46, Face2Face ~0.55) = honest thesis limitation, not a failure. | | | | | |
| 46 | 2026-06-26 | (pending) | **EXP2 lineage -> cross-domain GENERALIZATION (one new data axis) + paper-figure completeness (user: staged per-experiment improvement; "generalized deepfake detector, not a HiDF detector"; bundle must be write-up-complete).** Frozen P45 baseline first registered in `EXPERIMENT_REGISTRY.md` + git tag `p45-baseline` (rollback anchor). **Wiring catch that would have wasted a 12h run:** DANN is ON (`dann_enabled=True`), so every train sample routes through `get_domain_transform` (D0 clean/D1 jpeg/D2 noise/D3 blur) -- the `_build_train_pipeline`/real-aug paths are DEAD. The DANN domains cover jpeg/noise/blur but NOT resolution. **`--generalize_aug`** (new, default-OFF byte-identical) inserts a `RandomDownscale` (0.3-0.7x then upscale, p=0.40, class-symmetric) into EVERY train path incl. all 4 DANN domains -> adds the missing resolution-loss axis so the detector learns transferable artifacts instead of the HiDF pixel fingerprint; the additive head keeps explanation faithful-by-construction (the bet that breaks the old aug-vs-explanation trade-off: pre-P39 heavy aug lifted Celeb-DF 0.03->0.37 but crushed insertion 0.531->0.249). Code: `data/HiDF_transforms.py` (`RandomDownscale` + `_maybe_downscale`, threaded through `_build_train_pipeline`/`get_heavy_transforms`/`get_real_aug_transforms`/`_domain_0..3`/`get_domain_transform`/`get_transforms`), `data/HiDF_datasets.py` (4 call sites pass `config.generalize_aug`), `HiDF_config.py` (field+arg). **Paper-figure completeness (eval-only, fully guarded):** `metrics/HiDF_explanation.py` `contribution_del_ins` now exports the MEAN del/ins CURVE arrays (were AUCs only -> figure needed a re-run); suite plots `contribution_del_ins_curve.png` + `road_curve.png` + writes `faithfulness_summary.csv` (corrected headline), each in its own try/except so a cosmetic plot can never abort the run. Smoke (CPU): transforms 4 gates (OFF no-downscale byte-identical / ON fires in all 4 DANN domains+real+heavy / downscale degrades pixels + finite (2,3,224,224) / val-test never downscale), config merge (--generalize_aug->True, absent->False), metric curve export (50-pt finite curves) + plotting (PNG produced), py_compile 5 files. 1 notebook `Exp_A_p46_generalize_g80` (EXP2 recipe + `--generalize_aug`, 8ep, gamma 0.8); P45 notebooks KEPT (staged plan reuses EXP1/EXP3 as live references for their own next runs). | 0.977 | 0.534 | 0.878 contrib-fake | 0.396 | n/a | **RAN 6-26-26 2300hrs = PARTIAL WIN. generalize_aug fired (RandomDownscale active). IN-DIST + EXPLANATION IMPROVED; CROSS-DATASET DROPPED (downscale = wrong lever). See detail row.** |
| | | | **P46 OUTCOME detail.** Verified: `--generalize_aug` fired in TRAINING (the `aug_generalize=False` prints were the pre-train `HiDF_verify_dataset.py` sanity script, not the train run; `HiDF_train_real.py` command carried the flag + built train set with RandomDownscale(p0.4)). **IN-DIST IMPROVED:** AUC 0.972->**0.977**, fake@0.5 0.867->**0.901**, real 0.954->0.934, bal 0.911->**0.917**, opt_thr 0.346->0.439 (better calibrated). **EXPLANATION IMPROVED + FAITHFUL:** blended ins(fake) 0.832->**0.878**, del 0.513->**0.424**, gains +.132/.186->**+.182/.271**; head ins **0.902**/del **0.342**; m_t_std 0.0168->**0.0255** (sharper); faithful YES. The bet PAID OFF -- heavy aug SHARPENED rather than destroyed the additive head's explanation. **CROSS-DATASET REGRESSED (AUC, the honest measure):** Deepfakes 0.820->0.743, Face2Face 0.555->0.508, FaceSwap 0.457->0.435, NeuralTextures 0.670->0.613, Celeb-DF 0.563->0.534 (FaceShifter ~flat 0.657). Cross fake_acc@0.5 ticked up only as a threshold-shift artifact (model globally less real-biased; cross real_acc fell too). INSIGHT: the weakly-transferring cross signal is HIGH-FREQUENCY; downscale destroyed it -> degradation is the WRONG cross-dataset lever; the path is ADDING generic signal (seams/freq), not removing resolution. Paper figures (`contribution_del_ins_curve.png`/`road_curve.png`/`faithfulness_summary.csv`/`faithfulness_report.txt`) all generated. **P46 = new best in-dist + explanation model (promotion pending).** Also answered a user Q: explanation_metrics.json carries BOTH `intrinsic{}` (pixel-occlusion, OLD/confounded, ins 0.528/del 0.470) AND `contribution_space{}` (analytic, blended 0.878/0.424); faithfulness_report.txt surfaces only the correct contribution_space -> the "different scores" are confounded-vs-correct, not a bug. | | | | | |
| 47 | 2026-06-26 | (pending) | **Two PARALLEL config-only runs (user: "run both in parallel like two notebooks to save time").** No code change (all flags pre-existing + smoke-tested in P45/P46). **(A) `Exp_A_p47_sbi_seams_g80` = cross-dataset, DIFFERENT lever (NOT degradation).** EXP2 base (NO `--generalize_aug` -- P46 proved downscale hurts cross), one axis = stronger generic blend-seams: `lambda_sbi_cls 1.0->1.5`, `sbi_stride 6->4` (denser seams), `sbi_freq_mismatch 0.5->0.7`. SBI/Face-X-ray is the established cross-dataset method; P43 ran it PRIMARY (cls 2.0) and it backfired real-biased -- but WITHOUT calibration; here `lambda_calib 1.0` holds the operating point. Honest long shot. **(B) `Exp_B_p47_exp3_sharp_g80` = EXP3 staged.** Strong `lambda_aeh_sigconc 0.4` + `--generalize_aug` (P46-proven operating-point + explanation booster) = the definitive explanation-showcase model (sharpest deletion + fixed operating point); cross-dataset is NOT its job. Both cloned + flag-verified (raw-JSON check: A has sbi 1.5/4/0.7 + no generalize_aug; B has sigconc 0.4 + generalize_aug); nbformat-valid, outputs cleared. P46 result registered in EXPERIMENT_REGISTRY.md. | TBD | TBD | TBD | TBD | TBD | **shipped -- run A + B in parallel; A targets cross-dataset AUC/fake-recall up (esp. blend-based FaceShifter/FaceSwap) w/o real-bias collapse; B targets sharpest faithful explanation + fixed operating point** |

---

## Phase 26 — implementation detail

### A. Reverts (low-risk, Phase-25-collapse rollback)

| Flag | Phase 25 value | Phase 26 value | Why |
|---|---|---|---|
| `lambda_ins` | 1.0 | **0.5** | Hard top-K + lambda=1.0 dominated the gradient, crushing classification signal |
| `lambda_sparse` | 0.10 | **0.05** | Over-sparsified M_t into one-hot per frame, classifier lost spatial context |
| `alpha` (entropy in L_exp) | 0.0 | **0.05** | Removing entropy regulariser let M_t collapse with nothing pushing back |
| `beta` (TV in L_exp) | 0.1 | **0.5** | Less TV smoothing made M_t pointillistic; restored to standard |
| `bottleneck_hard_topk_frac` | 0.20 | **0.0** | Hard mask + class imbalance pushed model to "always predict fake" |
| `lambda_temp_sparse` | 0.05 | **0.02** | Pushed M_frame to one-hot on frame 0 (sample 1: 79% mass on one frame) |

### B. Bi-directional improvement
- `refine_gate` init: `-2.0` → `-0.5`.  Sigmoid(-2.0)=0.119 vs sigmoid(-0.5)=0.378.
- Phase 25 evidence: `refine_alpha` only moved 0.118→0.121 across all 8 epochs — the gate never engaged because every other knob was destabilising training.
- New init means cross_attention contributes ~38% to M_t from epoch 1, giving the gate gradient something to actually optimise.

### C. Class-balanced sampling
- `WeightedRandomSampler` with weights = `1/n_class` per sample.
- Optional flag `--class_balanced_sampler` (default True for HiDF where real:fake = 3488:3175).
- Phase 25 evidence: real_acc collapsed to 0.142 (375/437 reals flagged as fake) — model defaulted to majority class.

### D. Concept Slot Bottleneck (CBM)
- New module: `models/HiDF_concept_bottleneck.py`
- K=8 learned slot queries `Q_k`, dot-product attention over transformer features
- Each slot produces (1) a spatial attention map over (T·N) positions, (2) a pooled vector, (3) a scalar concept score via `slot_v`
- Final CBM logit = `Linear(K → 1)(concept_scores)`
- Combined with main logit via learnable blend `β` = sigmoid(blend), init 0.0 (50/50 blend)
- **Slot diversity loss** = mean off-diagonal cosine similarity between slot attention vectors; minimised → slots attend to *different* regions
- **CBM auxiliary classification loss** = focal-CE on `cbm_logit` alone (regularises the CBM head independently)
- Combined loss term: `lambda_cbm * (loss_cbm_aux + 0.5 * loss_cbm_div)`, `lambda_cbm` default 0.1

### Compute budget
- CBM parameters: K * d * 2 + K = 8 * 256 * 2 + 8 = ~4.1k extra params (negligible vs B4 backbone's 17M)
- CBM forward: K dot products + K softmaxes over T·N=784 positions + K scalar projections ≈ 1-2% wall-clock overhead
- Slot diversity loss: K² cosine similarities ≈ negligible

---

## Phase 35+ planned (NOT in this commit)

- **Phase 33 + 34 SHIPPED** (see ledger). P33 gave the detection breakthrough (0.971) but its seam target backfired on insertion; P34 is the architectural insertion fix (hard spatial top-k bottleneck) funded by that surplus, sweeping keep-frac 0.15/0.10 on the EXP3 base.
- **If P34 localizes but the headline insertion is still capped <0.70** (the bottleneck raises ins_gain_over_random above 0 — beats random — but the absolute blur-baseline AUC stays low): add the deferred **mean/black-baseline insertion** metric (eval-only; removes the global-blur shortcut that floors the absolute curve). Kept out of the training run to keep the end-of-run explanation suite crash-proof.
- **If P34 over-costs detection** at frac 0.10 (k=5): the 0.15 (k=8) arm is the fallback; or add a short keep-frac warmup (1.0→target over `faith_warmup_epochs`) — deliberately omitted in this commit (cold-start is safe: uniform M → top-k renorm ≈ uniform) to keep one axis and avoid an early-checkpoint train/eval mismatch.
- **Phase 35 — SSL pretraining** on a mixed deepfake corpus (DFDC + FaceForensics++ unlabelled subsets) before fine-tuning on HiDF.
- **Phase 36 — Test-Time Adaptation (TTA)**: at eval time, run K=10 SGD steps on entropy minimisation per cross-dataset batch.

## Metric protocol note (Phase 28)

The k1/k2/k4 frame-drop columns are NOT directly comparable across the P28 boundary: P≤27 numbers used zero-fill (gray frame — carries a +0.37 fake-prob artifact), P28+ headline numbers use nearest-frame replication. The legacy protocol is still computed every run under `k{K}_*_zerofill` keys, so a same-protocol comparison is always available. Similarly, del/ins gains `ins_gain_over_random` / `del_gain_over_random` (P28+) are the artifact-robust quantities to quote; the absolute del/ins AUCs remain confounded by the model's blur→fake response (blurred clip scores 0.82 vs 0.47 baseline).

## Metric protocol note (Phase 30)

1. **faithfulness_corr is not comparable across the P30 boundary.** P≤29 flattened all samples into one vector before the Spearman, so the ranking was dominated by BETWEEN-sample |gradient| scale (a quantity unrelated to map quality); P30+ uses the literature-standard per-sample rank correlation averaged over the subset (rank corr is scale-invariant per sample). Subset also grows 20 → 50.
2. **Known limitation to state in the paper**: loss_faith / loss_ins / loss_del all use the same Gaussian-blur construction as the del/ins evaluation protocol, so those eval numbers are partially *trained-for* (true of every method that optimises insertion at train time, e.g. RISE-style objectives). The untrained cross-checks are the replicate-fill and zero-fill k-drop tests and `mt_vs_random_model_cosine` — quote those alongside.

## Metric protocol note (Phase 31)

1. **Quote fake-only + gains, not mixed absolutes, for del/ins.** A LOW insertion curve is CORRECT for real samples (revealing real pixels should keep fake-prob low), so the mixed-class absolute insertion AUC is bounded near ~0.45 by design and can never reach the ≥0.65 target; the well-defined quantities are `insertion_auc_fake_only`, `deletion_auc_fake_only`, `ins_gain_over_random`, `del_gain_over_random`. The targets table maps to: fake-only insertion ≥0.65, fake-only deletion ≤0.32, both gains >0.
2. **Single-frame k-drop targets are retired for HiDF.** Replicate-fill copies a neighbouring frame back in; on fully-fake, temporally-redundant 16-frame clips the neighbour carries the same class evidence, so the measured quantity is physically ≈0 for ANY map — the k1≥2.5 target is unachievable by construction, not by model deficiency (P29 +5.12 and P30 −19.2 are the same noise). The temporal-faithfulness claim rests on `k*_blurfill` (evidence-destroying fill, new headline), eff_fr concentration, and the qualitative overlays. Caveat for the paper: blur-fill shares the Gaussian operator family with the trained losses (frame-level vs region-level), so replicate/zero k-drops + `mt_vs_random_model_cosine` remain the fully-untrained checks.

---

## Standing rule (lesson from Phase 25)

**Change one architectural axis per phase.**
Phase 25 bundled seven simultaneous amplifications and we couldn't attribute which knob caused the collapse.  Phase 26 changes only the CBM axis (plus reverts that *undo* Phase 25); future phases ship one new architectural direction at a time so the ablation table has clean attribution.

*Phase 30 exception (user-directed consolidation, 2026-06-12):* three repairs ship together by explicit instruction ("fix it once and for all"). Attribution is preserved differently: each repair has an independent off-switch (`--lambda_del 0.0`, `--lambda_temp_band 0.0`; slot init is structural but its effect is isolated by `cbm_div`) and a dedicated log signature (`cbm_div` falling = slot repair working; `del=` falling after warmup = D-pass working; `eff_fr`→4–6 with `tband`→0 = band working), so a post-hoc per-axis ablation remains runnable.

*Phase 31 exception (same directive, 2026-06-12):* the P30 run proved the mechanisms but exposed a poisonous loss form, a spatial ratchet, and a focal-alpha no-op; P31 repairs all three plus an eval-only fill protocol under the same standing permission. Off-switches: `--lambda_del 0.0` / `--del_anchor_every 0`, `--lambda_spatial_band 0.0`, unset `--focal_alpha_pos/neg` (bit-exact legacy focal), blur-fill is additive eval-only. Log signatures: `sband` + `eff_sp` (band), `del=` + val fake_acc at E4 (detox), fake_acc trajectory (focal), `k*_blurfill` keys (fill).

## Standing rule (lesson from Phase 28)

**Couple through renormalised forms only, and smoke-test trainability, not just gradients.**
1. Any mechanism that makes attention maps load-bearing must preserve feature
   MAGNITUDE no matter how peaky the maps get: convex pooling (`Σ wᵢ·xᵢ` with
   `Σ wᵢ = 1`) or softmax-renormalised log-priors. Multiplicative masking of
   token magnitudes is banned — peaky maps + softmax attention over the masked
   tokens = dilution → constant output → dead cls gradient → the sparsity
   regularisers collapse the maps unopposed (the P28 one-way ratchet).
2. A pre-push smoke test must include (a) per-sample logit VARIANCE relative
   to a known-good mode, and (b) a ~30-step mini-overfit asserting the loss
   actually decreases. P28's smoke verified gradient existence at init — true
   but insufficient; either check above would have caught the death before it
   cost an 8-hour Kaggle run.
